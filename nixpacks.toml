[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["COREPACK_INTEGRITY_KEYS=0 corepack enable && COREPACK_INTEGRITY_KEYS=0 corepack prepare pnpm@10.4.1 --activate && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node start.mjs"
